Sci-Fi Valley Con Attendee App V4.71 — Top-Left Back Button

Rolled forward cumulatively from V4.70 In-App Back Button.

CHANGED
- Moves the small circular in-app Back button from the upper-right side of the header to the upper-left corner.
- The Back button now appears immediately before the Sci-Fi Valley Con app logo/brand so it is the left-most control in the top bar whenever navigation history exists.
- The button still hides automatically when there is no previous in-app screen.
- Screen-history behavior and previous-page scroll-position restoration are unchanged.
- The Back arrow remains fixed-size and excluded from accessibility scaling so it stays centered from 100% through 200% Text Size.

PRESERVED
- V4.70 in-app navigation history and scroll restoration.
- V4.69 reliable external IMDb links.
- V4.67 Lost & Found inquiry and reporting integration.
- V4.66 stacked Wikipedia popup.
- V4.65 Event Guide viewport centering.
- All prior text scaling, icon alignment, anti-bounce scrolling, guest-name fitting, reporting, schedule, map, notification and PWA features.

CACHE
- App build version and service-worker cache bumped to V4.71.
- CSS, Event Guide JS and app JS cache-busting query strings bumped to V4.71.

INSTALL
Upload the files inside this ZIP over the matching files in the attendee-app repository.
No Admin App or Notify Worker changes are required.
