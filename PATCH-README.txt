Sci-Fi Valley Con Attendee App V4.72 — Top-Bar Name 115% Cap

Rolled forward cumulatively from V4.71 Top-Left Back Button.

CHANGED
- Keeps the in-app Back arrow in the upper-left corner exactly where it was placed in V4.71.
- Lowers the accessibility scaling ceiling for the top-bar SCI-FI VALLEY CON brand text from 130% to 115%.
- When the overall Text Size setting is 130%, 150%, 175% or 200%, only this compact top-bar event name stops at 115% so it cannot slide underneath or collide with the Program Tools tab.
- The date/location line and normal readable app content continue following their existing accessibility scaling rules.

PRESERVED
- V4.71 top-left Back button.
- V4.70 in-app navigation history and scroll restoration.
- V4.69 reliable external IMDb links.
- V4.67 Lost & Found inquiry and reporting integration.
- V4.66 stacked Wikipedia popup.
- V4.65 Event Guide viewport centering.
- All prior text scaling, icon alignment, anti-bounce scrolling, guest-name fitting, reporting, schedule, map, notification and PWA features.

CACHE
- App build version and service-worker cache bumped to V4.72.
- CSS, Event Guide JS and app JS cache-busting query strings bumped to V4.72.

INSTALL
Upload the files inside this ZIP over the matching files in the attendee-app repository.
No Admin App or Notify Worker changes are required.
