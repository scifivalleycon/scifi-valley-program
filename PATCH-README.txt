Sci-Fi Valley Con Attendee App V4.39 — Con-Quest Orange Button Fix

This specifically targets the orange button shown inside the vendor information popup:

★ CON-QUEST PARTICIPANT

The entire orange button is now the clickable/tappable Con-Quest information control.

Behavior:
- Tap the orange CON-QUEST PARTICIPANT button.
- The vendor popup closes.
- The dedicated WHAT IS CON-QUEST? explanation opens.
- This avoids stacking two dialogs and is more reliable on phones.
- The button keeps the existing compact orange appearance instead of adding extra
  WHAT IS THIS text inside the badge.
- Mobile tap target is at least about 42px tall.

The explanation continues to cover how Con-Quest works, the charity purpose,
participating tables, bottle caps, raffle tickets and the Gearbox Union starting point.

No Admin or Notify Worker update is required.
No backend-managed JSON files are included.

UNZIP FIRST, then upload all files inside the ZIP.
