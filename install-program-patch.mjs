import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";
const here=path.dirname(fileURLToPath(import.meta.url)),target=process.argv[2]?path.resolve(process.argv[2]):process.cwd(),files=path.join(here,"files");
for(const name of["sfvc-vendor-program.js","sfvc-vendor-program.css"])fs.copyFileSync(path.join(files,name),path.join(target,name));
const index=path.join(target,"index.html");if(!fs.existsSync(index))throw new Error(`index.html not found in ${target}`);let html=fs.readFileSync(index,"utf8");if(!html.includes("sfvc-vendor-program.css"))html=html.replace(/<\/head>/i,'  <link rel="stylesheet" href="sfvc-vendor-program.css?v=1.0">\n</head>');if(!html.includes("sfvc-vendor-program.js"))html=html.replace(/<\/body>/i,'  <script src="sfvc-vendor-program.js?v=1.0"></script>\n</body>');fs.writeFileSync(index,html);console.log("SFVC Vendor Program integration installed without replacing app.js, styles.css, data, maps, schedules, guests, vendors, or existing features.");
